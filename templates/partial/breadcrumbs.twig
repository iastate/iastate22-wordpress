<nav class="iastate22-breadcrumb" aria-label="Breadcrumb">
  <ol class="iastate22-breadcrumb__list">
    {% if is_preview %}
      <li><a href="#">Home</a></li>
      <li><a href="#" aria-current="page">{{ function('get_the_title')|e('esc_html') }}</a></li>
    {% else %}
      {{ function('the_breadcrumb') }}
    {% endif %}
  </ol>
</nav>