<div class="paragraph-widget paragraph-widget--link-block">
    <div class="link-block">
        <h2>{{ fields.title }}</h2>
        <div class="link-set">
            <ul>
            {% for link in fields.links %}
                <li>
                    <a href="{{ link.url }}" class="link-set__link iastate22-link-secondary">{{ link.label }}<span class="arrow"></span></a>
                </li>
            {% endfor %}
            </ul>
        </div>
    </div>    
</div>