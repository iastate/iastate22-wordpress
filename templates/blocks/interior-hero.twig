{% if function('is_singular') %}
<div class="hero"{% if block.anchor|length > 0 %} id="{{ block.anchor|e('esc_attr') }}"{% endif %}>
	<div class="hero__text-wrap">
		{% include ['templates/partial/breadcrumbs.twig'] %}
		<h1 class="hero__title">
			{% if fields.hero_title %}
				{{ fields.hero_title }}
			{% else %}
				{{ wp_title }}
			{% endif %}
		</h1>
	</div>
	{% if fields.hero_image %}
		<div class="hero__media">
			{{ media_image(fields.hero_image, {size:'full', placeholder: true}) }}
		</div>
	{% endif %}

	{% if fields.subnav_toggle == true %}
		<div class="hero__subnav-wrap">
			{% include ['templates/partial/subnav.twig'] %}
		</div>
	{% endif %}
</div>
{% endif %}
