{% if function('is_singular') or is_preview %}
	<div class="hero"
			{%- if block.anchor|length > 0 %} id="{{ block.anchor|e('esc_attr') }}"{%- endif -%}>
		<div class="hero__text-wrap">
			{% include ['templates/partial/breadcrumbs.twig'] %}
			<h1 class="hero__title" {{ acf_inline_text('hero_title') }}>
				{% if fields.hero_title %}
					{{ fields.hero_title }}
				{% else %}
					{{ function('get_the_title') }}
				{% endif %}
			</h1>
		</div>
		{% if fields.hero_image %}
			<div class="hero__media" {{ acf_inline_toolbar('hero_image') }}>
				{{ media_image(fields.hero_image, {size:'full', placeholder: true}) }}
			</div>
		{% endif %}
		{% if fields.subnav_toggle == true -%}
			<div class="hero__subnav-wrap" {{ acf_inline_toolbar('subnav_toggle') }}>
				{% include ['templates/partial/subnav.twig'] %}
			</div>
		{%- endif %}
	</div>
{% endif %}
