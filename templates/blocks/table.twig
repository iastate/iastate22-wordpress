<div class="paragraph-widget paragraph-widget--table">
    <div class="iastate22-table" role="region" aria-labelledby="{{ fields.table_caption.caption_id }}" tabindex="0">
    <table>
        <caption id="{{ fields.table_caption.caption_id }}">{{ fields.table_caption.caption }}</caption>
        <thead>
            <tr>
                {% for header in fields.table_headers %}
                    <th>{{ header.header }}</th>
                {% endfor %}
            </tr>
        </thead>
        <tbody>
            {% for row in fields.table_rows %}
                <tr>
                    {% for column in row.table_column %}
                        <td>{{ column.column_copy }}</td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </tbody>
    </table>
    </div>
</div>
